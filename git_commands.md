1. git init = initialise version control in directory 
2. git status = get status of files in a directory 
3. git commit -a = 
4. git checkout =
5. git diff = get difference between file in dir and snapshot
